import ROUTES from './routes'

enum LocationStatus {
  LOADING = 'loading',
  READY = 'ready',
  DENY = 'deny'
}

enum ButtonType {
  PRIMARY = 'primary'
}

export { ROUTES, LocationStatus, ButtonType }
